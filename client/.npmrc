# @asseinfo/react-kanban (built on the unmaintained react-beautiful-dnd) still
# declares React 16/17 peer ranges, so npm 7+ rejects the React 18 install.
# The library works fine at runtime on React 18, so we relax peer resolution.
legacy-peer-deps=true
